
<%= bootstrap_form_for([@forum_thread, @post], :html => {:class => "form-horizontal"}) do |f| %>
  <%= f.text_area :body, cols: 40, rows: 10 %>
  <%= f.select :mode, @forum_thread.modes %>
  <div class="actions form-actions">
    <%= f.submit "Contribute comment" %>
  </div>
<% end %>

